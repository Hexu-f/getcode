package $params.entityPackageName;

#if($params.inputDate == 1)
import java.util.Date;
#end

/**
 * This document is automatically generated by the program
 * Created by Get Code generator on ${params.nowDate}.
 * If you have any questions, please contact fhx35@hotmail.com
 */
public class ${params.entityName} {
#foreach($col in $params.columns)
    #if($col.comment)
    //$col.comment
    #end
    private ${col.propertyType} ${col.propertyName};
#end

#foreach($col in $params.columns)

    public ${col.propertyType} get${col.firstMaxPropertyType}() {
        return ${col.propertyName};
    }

    public void set${col.firstMaxPropertyType}(${col.propertyType} ${col.propertyName}) {
        this.${col.propertyName} = ${col.propertyName};
    }
#end

}